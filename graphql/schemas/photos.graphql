type PhotoQuery {
    activePhotos: [PhotosRef]
    trashedPhotos: [PhotosRef]
}

type PhotosRef {
    location: String
    description: String
    createdAt: Float
    favourite: Boolean
    trashed: Boolean
    size: Int
}